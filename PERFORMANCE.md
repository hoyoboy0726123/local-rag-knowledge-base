# 本機 RAG 的效能調校筆記

這份筆記記錄的是**在單張消費級顯卡上跑本機 RAG** 時遇到的效能問題與解法。
每一項都附實測數字，都不是「程式寫得慢」，而是**推理引擎的資源調度與快取行為**
造成的——所以在別的專案上也會遇到。

測試環境：RTX 5060 Laptop 8 GB（Ollama 實際可用 6.4 GiB）、Ollama 0.32.3、
qwen3:8b（生成）＋ bge-m3（向量）＋ 25 個切片的小知識庫。

**成果：一題從 35–40 秒降到 10–20 秒，知識庫沒有的問題從 30 秒降到 3–7 秒。**

---

## 1. 模型互相驅逐（影響最大）

### 症狀

每問一題都很慢，而且慢得不穩定。拆開計時後發現「向量檢索」要 11–14 秒，
但 25 個切片的向量搜尋應該是毫秒級的。

### 診斷

連續呼叫 embedding 三次，中間不做生成：

```
第 1 次  3.69s   ← 冷載入
第 2 次  0.23s   ← 暖的，16 倍快
第 3 次  0.23s
```

再夾一次生成：

```
生成       6.81s   載入=[qwen3:8b]     ← bge-m3 被踢掉了
之後 embed 4.44s   載入=[bge-m3]       ← 又要重載
```

`ollama ps` 從頭到尾**只顯示一個模型**。看 Ollama 的日誌：

```
gpu memory  available="6.4 GiB"
```

顯卡總共 8.15 GB，桌面環境佔掉約 1.2 GB，Ollama 只剩 6.4 GiB 可用。
而 qwen3:8b 在 8192 上下文下要 6.2 GB——**塞不進 bge-m3 的 0.7 GB**。

問答的每一輪都是「先 embed、再生成」，於是**每問一題就要重載兩次模型**，
其中生成模型有 6.2 GB。

### 解法

把向量模型釘在 CPU 上，讓生成模型能一直留在顯卡：

```python
json={"model": model, "input": texts, "options": {"num_gpu": 0}}
```

### 效果

|          | embed        | 生成         | 每輪合計 |
|----------|--------------|--------------|----------|
| 走 GPU   | 2.9~3.8s     | 4.1~4.7s     | 約 7.9s  |
| **走 CPU** | **0.26s（暖）** | **0.27s（暖）** | **約 0.55s** |

`ollama ps` 也直接看得到差別：走 GPU 時永遠只有一個模型，走 CPU 之後兩個並存。

代價幾乎沒有：向量模型只處理一個短問句，工作量小，CPU 上暖機後 0.26 秒，
跟它在 GPU 上的 0.23 秒相當。佔用的是系統記憶體。

> **可移植的原則**：小顯存機器上跑多個模型時，把「小而頻繁」的模型移到 CPU，
> 讓「大而昂貴」的模型獨佔顯卡。判斷依據是**重載成本**，不是模型本身的推理速度。

---

## 2. 上下文視窗被安靜截斷

### 症狀

模型有時候不回答問題，而是把系統提示複述一遍；有時候答到一半就斷掉。

### 診斷

同一組訊息，只改 `num_ctx`：

```
num_ctx=4096 ：prompt 只剩 820 token  → 檢索結果整段消失
num_ctx=8192 ：prompt 4147 token      → 完整，正常作答
num_ctx=16384：prompt 4147 token      → 與 8192 相同
```

**同樣的輸入，prompt_eval_count 卻是 820 對 4147。** 超出視窗的部分不會報錯，
而是被安靜丟掉。模型手上沒有任何資料，於是複述系統提示當答案。

另一種症狀是提示剛好塞得下、卻沒有空間生成：某題 prompt 4047 token、
只剩 49 個可用，答案講到一半就 `done_reason=length` 斷掉。

**調 `num_predict` 碰不到這個瓶頸**——實際可生成的量是 `num_ctx` 減掉提示詞。

### 解法

依提示詞長度動態選 `num_ctx`，**量化成級距而不是算出精確值**：

```python
CTX_STEPS = (8192, 16384, 32768)
CHARS_TO_TOKENS = 0.65      # 中英混排實測值
CTX_RESERVE_TOKENS = 2048   # 留給生成
```

級距化是必要的：Ollama 每遇到一個沒見過的 `num_ctx` 就會重新載入模型，
逐題微調等於每題都重載。

> **可移植的原則**：不要沿用推理引擎的預設上下文。先量自己的提示詞有多長，
> 再決定。截斷是**靜默**的，症狀會偽裝成「模型很笨」。

---

## 3. 多輪檢索重複餵同樣的內容

### 症狀

列舉型問題極慢，答案還會被截斷。`ollama ps` 顯示模型被載入成 32768 上下文、
佔 10 GB、41% 跑在 CPU 上。

### 診斷

Agent 迴圈允許模型換關鍵詞多查幾次，而**每一輪的檢索結果都累積在對話裡**。
知識庫的切片數少於 top_k 時，第二次檢索撈到的是同一批東西——資訊量是零，
上下文卻翻倍。動態 `num_ctx` 忠實地跟著放大，KV cache 就撐爆了。

### 解法

跨輪次去重，已經給過的切片不再重複餵：

```python
fresh = [c for c in chunks if not citations.seen(c.chunk_id)]
if not fresh:
    return "這次檢索到的內容前面都已經提供過了，請改用明顯不同的關鍵詞。", 0
```

### 效果

```
累積上下文  30,885 字 → 10,421 字（−66%）
num_ctx     32768 → 16384
```

> **可移植的原則**：Agent 迴圈的工具回傳會累積，要主動去重。
> 回一句「沒有新資料」比重倒一次更有用——它同時告訴模型「換個詞或直接作答」。

---

## 4. 重排序在小知識庫上是純成本

### 診斷

| | 檢索耗時 |
|---|---|
| 開啟重排序 | **6.05s**（三次量測 5.96 / 6.05 / 6.11） |
| 關閉重排序 | **0.27s**（0.24 / 0.27 / 0.29） |

22 倍差距。原因是 cross-encoder 要對 18 個候選逐一算分，而且跑在 CPU 上
（GPU 被生成模型佔滿）。

品質呢？同一組六題，**開與關都是 6/6 通過**。它確實改變了檢索結果
（7 題有 6 題撈到不同的切片），但沒有反映到答案品質上。

原因是知識庫只有 25 個切片，候選池 18 段已經涵蓋七成——它做的不是「篩選」，
而是把整個知識庫重排一遍。

### 解法

候選池涵蓋知識庫一大半時跳過，用比例而不是絕對值：

```python
if corpus_size and len(chunks) * 2 >= corpus_size:
    return chunks[:top_k]
```

知識庫長大後重排序會自動回來工作，管理員不需要記得去切換開關。

> **可移植的原則**：重排序的價值來自「從一大堆候選裡撈出埋在後面的那一段」。
> 候選池相對語料庫夠小時它才有意義，否則是純成本。

---

## 5. 反直覺：送得少不等於跑得快

### 假設

相關性把關要把約 6,000 字的檢索內容送給模型判斷，耗時 9 秒。
直覺上只送每段開頭（700 字 × 6 段）應該會快很多。

### 實測

|              | 通過  | 把關耗時 | 總計   |
|--------------|-------|----------|--------|
| 取樣 700 字  | 7/7   | 27.4s    | 93.2s  |
| **完整內容** | 7/7   | **10.7s** | **58.1s** |

**取樣慢了 2.5 倍。**

### 原因

提示快取。完整的檢索內容本來就在對話裡（作答要用），把關再送一次同樣的文字
就命中快取；取樣則是把內容**切碎重組成一份全新的輸入**，對快取而言沒見過，
得整份重新 prefill。

取樣還踩過準確度的坑：240 字時有題目被誤擋，因為答案藏在表格下方的一行小字，
只看開頭根本看不到。

> **可移植的原則**：在有前綴快取的推理引擎上，**重複使用完全相同的前綴**
> 比「精簡輸入」更有效。要優化就優化到「與前一次請求共用前綴」，
> 而不是把輸入改小改碎。

---

## 6. 測量方法論：先消除雜訊源

這次調校中，有一次測量得出「關掉重排序反而慢 3 倍」的荒謬結論
（33.3s vs 101.6s）。

原因是當時模型還在互相驅逐，**每題 4–8 秒的隨機重載變異，淹沒了重排序
那 6 秒的真實差異**。

修好模型驅逐之後重測，數字才穩定下來。

### 建議的順序

1. **先找出並消除變異最大的雜訊源**（這次是模型重載）
2. 再量各個階段的成本
3. 每個設定跑 3 次以上，看 min/avg/max —— 變異大就代表還有沒找到的雜訊
4. 端對端計時只用來確認方向，**細部差異要拆階段量**

拆階段的做法很簡單，把要量的函式包起來就好：

```python
_orig = module.func
def timed(*a, **k):
    start = time.time()
    r = _orig(*a, **k)
    timings["func"] += time.time() - start
    return r
module.func = timed
```

---

## 附：一題的時間都花在哪（調校前 vs 調校後）

調校前（qwen3:8b，「料件有哪些種類」，總計 37.8s）：

| 階段 | 耗時 | 畫面顯示 |
|---|---|---|
| 決定要檢索 | 1.6s | 思考中… |
| 向量檢索（含重排序、模型重載） | 14.2s | 思考中… |
| 相關性把關 | 10.3s | 思考中… |
| 首字延遲 | 4.1s | ◐ 檢索（仍無文字） |
| 逐字輸出 | 7.6s | 文字出現 |

**「思考中…」佔了 26 秒**——使用者在那段時間看不到任何進度。

調校後同一題約 15–20 秒，其中檢索 0.3 秒、把關 2–3 秒。

> 順帶一提，「思考中…」涵蓋了三個不同階段（決定檢索、檢索、首字緩衝），
> 而檢索的進度事件是**檢索完成後**才送出的。若要改善體感，把事件改成
> 檢索**開始前**就送，空窗會從 26 秒縮到 1 秒——這是零成本的改善。
