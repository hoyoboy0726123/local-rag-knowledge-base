# 軟體設計文件 (SDD) — RD PM 研發管理知識庫 **V2**

## 給 AI Coding Agent 的話（**動工前先讀這段**）

這份文件描述 V2：**React + FastAPI 取代 Streamlit**，重點在做出一個上得了檯面的介面。

**V1 已經是一份可運行、73+ 項測試全過的實作**（見上層目錄的 `SDD.md`）。
V2 換的是**呈現層**，不是重做整個系統：

| 層 | V1 → V2 |
|---|---|
| 資料模型、向量表 | 複製一份，結構完全相同 |
| 解析、切片、索引管線 | 複製一份，未修改 |
| 檢索、Agent 迴圈、Grounding | 複製一份，僅 system instruction 加一條（見 §9.6） |
| 權限與對話隔離 | 邏輯不變，改以 JWT 驗證（`auth_service` 需去除 Streamlit 相依） |
| 呈現層 | Streamlit → React + FastAPI |

> **V2 是完全獨立的專案。** 自己的 venv、`knowledge.db`、`services/`、
> 範例知識庫，可以單獨打包分享，不依賴 V1 的任何檔案。
> 代價是同一個 bug 要修兩次——這是為了「兩版能各自獨立交付」付出的成本。

**因此上層 `SDD.md` 的 §7 所有實作陷阱仍然全部適用**，尤其是：
`127.0.0.1` 不可寫 `localhost`、生成請求帶 `think: False`、
向量維度要先探測對齊、相關性交給 LLM 判斷、
Agent 的三層強制檢索防護、模型選單不可靜默改設定。
**那些坑跟 UI 框架無關，換成 React 一樣會踩。**

V2 新增的必讀事項在 §9。

---

## 0. 文件資訊

| 項目 | 內容 |
|------|------|
| 文件版本 | 2.0 |
| 建立日期 | 2026年8月2日 |
| 對應 V1 | `../SDD.md`（Streamlit 版，已對齊實作） |
| 技術棧 | React 18 + Vite + FastAPI + SQLite + sqlite-vec + Ollama |
| 部署 | 區網伺服器單一部署，前端建置後由 FastAPI 提供靜態檔 |

---

## 1. 為什麼要做 V2

### 1.1 Streamlit 的三個結構性限制

不是「不好看」而已，是**架構層面做不到**：

**一、每次互動都重跑整個腳本。** 這是 Streamlit 的核心模型。
後果是每個 widget 的狀態都要靠 `session_state` 手動維護，
而且不能在 widget 建立後修改它的狀態（V1 §5.4 陷阱）。
知識庫的介面有大量局部更新（展開來源、切換切片、編輯關鍵字），
每一次都重跑整頁在體感上就是「卡一下」。

**二、版面被框死。** Streamlit 的排版單位是由上而下的區塊流，
做不出「左側清單 + 右側詳情 + 常駐來源面板」這種三欄協作版面——
而那正是知識庫最自然的操作結構（RAGFlow、Onyx 都是這個佈局）。

**三、元件的視覺無法細調。** 主題只能改幾個色票，
間距、圓角、字重、狀態層次都套用官方樣式。要做出專業感就必須注入 CSS，
而那等於在跟框架對抗。

### 1.2 V2 想達到的具體目標

* **回答與來源並排**，點引註編號能同步高亮右側對應來源
* **切片檢視是三欄工作台**，左清單、中內容、下方關鍵字編輯，切換不重載
* **串流回覆逐字出現**，不是整段跳出來
* 視覺上**不使用深色 AI 套版風**（見 §3）

---

## 2. 系統架構

```
瀏覽器 (React SPA)
      │  fetch / SSE / WebSocket
      ▼
FastAPI  ── /api/*        REST + SSE 串流
         └─ /            前端建置產物 (frontend/dist)
      │
      ├── services/        ★ 自 V1 複製，僅 auth_service 去除 Streamlit 相依
      │     agent_service / rag_service / ingest_service
      │     ollama_client / auth_service / chat_service / stage_service
      ▼
SQLite (knowledge.db) + sqlite-vec ──► Ollama (127.0.0.1:11434)
```

**`services/` 幾乎不用改，但不是完全不用。**

實作時才發現 `auth_service` **匯入了 streamlit**——
它用 `st.session_state` 存登入狀態、用 `st.stop()` 中斷頁面。
V2 沒有 streamlit，一啟動就 `ModuleNotFoundError`。

處理方式：把 `login` / `logout` / `current_user` / `require_login` /
`require_admin` 從 `auth_service` 移除，只保留**與框架無關**的部分
（雜湊、驗證帳密、帳號管理）。「目前是誰」與「擋不擋下來」
交給 `backend/deps.py` 以 JWT + 依賴注入處理。

其餘六個 service（`agent` / `rag` / `ingest` / `ollama_client` /
`chat` / `stage`）確認**沒有任何 streamlit 相依**，原封搬過來就能用。

### 2.1 目錄結構

```
v2/
  backend/
    main.py          FastAPI 應用 + 掛載前端靜態檔
    api/
      auth.py        登入、JWT、current_user 依賴注入
      chat.py        問答（SSE 串流）、對話紀錄
      stages.py      階段導覽、文件閱讀與下載
      admin.py       索引、切片、關鍵字、模型設定、帳號
    deps.py          共用依賴（require_login / require_admin）
  frontend/
    src/
      main.jsx       路由掛載
      app/           Layout.jsx（骨架＋可收折側邊欄）、api.js（fetch 與 SSE）
      features/      Chat / Stages / Library / Models / Users / Profile / Login
      styles/        tokens.css ★ 設計 token 唯一來源、app.css
  services/          自 V1 複製（見 §2 說明）
  models.py  database.py  seed_data.py
  knowledge.db       ★ 自己的資料庫，與 V1 互不影響
  sample_knowledge_base/
  venv/              ★ 自己的虛擬環境
  start.bat          一鍵安裝並啟動（單一行程）
  test_api.py        API 層測試（53 項）
```

> **不要另外開 `components/` 放共用元件。** 這個規模（7 個頁面）拆不出真正被重用的元件，
> 硬拆只會多一層轉介。共用的是 **CSS class**（`.panel` `.btn` `.row` `.note`），
> 不是 React 元件——設計一致性由 `tokens.css` 與 `app.css` 保證。

---

## 3. 設計系統

### 3.1 為什麼不用深色 AI 套版

目前 AI 產品有一種高度同質的視覺：深色底、紫藍漸層、發光邊框、玻璃擬態。
**這套視覺不適合這個系統**，理由不是喜好：

* 使用者是 RD PM，**在辦公室日光環境下長時間閱讀規範文件**。
  深色底閱讀長篇中文的疲勞度明顯較高。
* 這是**內部工具**，不是要營造科技感的展示品。
  它應該像 Notion、Linear、Outline 那樣安靜、可長時間使用。
* 漸層與發光會**干擾內容層次**。知識庫最重要的視覺任務是
  「讓人分辨哪些是原文、哪些是 AI 的話、哪些是來源」——
  裝飾性效果會稀釋這個分辨。

### 3.2 色彩

以**暖中性灰**為底，不用純白也不用藍灰（藍灰正是 AI 套版的味道）。
強調色只有一個：**深青綠**，用於品牌、引註、可信任的狀態。

```css
/* frontend/src/styles/tokens.css —— 唯一的色彩來源 */
:root{
  /* 底層：暖中性，長時間閱讀不刺眼 */
  --bg:          #FBFAF7;   /* 頁面底 */
  --surface:     #FFFFFF;   /* 卡片、面板 */
  --raised:      #F5F4F0;   /* 側邊欄、表頭、次要區塊 */
  --line:        #E6E3DC;   /* 一般分隔線 */
  --line-strong: #D4D0C6;   /* 輸入框、按鈕邊界 */

  /* 文字：三階，不用純黑 */
  --ink:   #1C1B18;   /* 主要內容 */
  --ink-2: #5C5950;   /* 內文、次要 */
  --ink-3: #8B877C;   /* 標籤、metadata */

  /* 強調：深青綠 = 品牌 + 引註 + 已驗證 */
  --accent:      #0F6F62;
  --accent-soft: #E4F0ED;
  --accent-line: #B8D8D1;

  /* 語意色：低彩度，避免螢光感 */
  --amber:  #9A6B08;  --amber-soft:  #FBF2DE;  --amber-line:  #EBD9A8;
  --danger: #A03530;  --danger-soft: #FBEAE8;  --danger-line: #F0CFCC;
  --ok:     #2E9E6B;

  --r: 10px;  --r-sm: 7px;
  --shadow: 0 1px 3px rgba(28,27,24,.05);
}
```

**語意色的用途要一致，不可混用**：

| 色 | 只用於 |
|---|---|
| 青綠 | 引註編號、來源、已驗證、主要動作 |
| 琥珀 | 需要人介入但系統仍可運作（缺 VLM、等待模型） |
| 紅 | 真正的失敗（解析失敗、驗證未通過） |

V1 有一個實際教訓：把「缺少視覺模型」和「檔案解析失敗」混在同一個
錯誤清單裡，管理員會以為檔案壞了。**顏色也一樣不能混。**

### 3.3 字體與間距

```css
--sans: "Inter", "Noto Sans TC", -apple-system, "Segoe UI", sans-serif;
```

* 中英混排時 `Inter` 負責英數、`Noto Sans TC` 負責中文，
  字高一致不會出現「英文偏小」的錯落感。
* 內文 14px / 行高 1.6；中文長段落**不要低於 1.6**。
* 間距一律 4 的倍數。

### 3.4 三個必須存在的視覺區辨

知識庫的介面若只做到「好看」而沒做到這三點，就是失敗的：

1. **AI 的話 vs 文件原文**——原文一律在來源面板／切片檢視中呈現，
   底色與邊界都與回答區不同。
2. **引註可點且可回溯**——`[1]` 是實心色塊而非純文字，
   點擊會高亮右側對應來源。
3. **檢索過程可見**——「🔎 檢索：<關鍵詞> — 取回 N 段」以膠囊呈現，
   讓使用者知道 AI 做了什麼，而不是黑箱吐答案。

---

## 4. 頁面與版面

### 4.1 全域骨架

```
┌──────────┬────────────────────────────────────────┐
│ 側邊欄    │  頁首（標題 + 頁面層級操作）              │
│ 230px    ├────────────────────────────────────────┤
│ 固定      │  內容區（各頁自訂）                       │
│          │                                        │
│ 底部：    │                                        │
│ 引擎狀態  │                                        │
│ 使用者    │                                        │
└──────────┴────────────────────────────────────────┘
```

側邊欄分「工作區」與「管理」兩組，**管理組僅管理員可見**
（後端一併驗證，前端隱藏只是體驗）。底部常駐 Ollama 連線狀態，
V1 的經驗是這個狀態必須隨時看得到，否則使用者不知道為什麼問答壞了。

**側邊欄可收折**（230px → 56px），狀態記在 `localStorage`。

> **收折成圖示列，不要整條藏掉。** 藏掉之後使用者得先想「怎麼把它叫回來」
> 才能換頁；留著圖示則隨時點得到。代價是每個項目都要補 `title`，
> 否則收折後只剩看不懂的符號。
>
> **導覽項在收折後要給固定的方形尺寸**（38×38）。只寫 `justify-content: center`
> 不夠——側邊欄收折時是 `align-items: center`，`.nav` 會縮到內容寬，
> 連帶讓連結只剩圖示的 14px 寬、高度卻仍是 41px，
> 選取態的底色就被擠成一條豎線。
>
> **收折鈕本身要有實體邊框與底色**（展開 28×28、收折 38×38 對齊圖示列）。
> 它是收折後整條側邊欄唯一的操作，做成只有一個符號的隱形熱區會小到找不到。
>
> 收折的實質收益：問答頁是三欄版面，1280px 下答案欄從 415px 變成 589px（+42%）。
> 這是「讓出空間」而不是「少一個東西」。

### 4.2 AI 問答（主畫面）

**三欄：對話清單（224px）｜ 對話串（自適應）｜ 來源面板（308px，sticky）**

兩側都是 sticky，中間捲動。清單可收合，收合後退回兩欄——
1400px 以下三欄會擠，把決定權交給使用者而不是猜螢幕寬度。

> **內容區要滿版，不要用 `max-width` + `margin: 0 auto` 置中。**
> 固定上限置中會在左右各留一道等寬空白（1460px 上限、1996px 螢幕實測各 262px）。
> 側邊欄收折之後，那道空白就緊貼著細細的圖示列，看起來像版面破圖；
> 更糟的是**收折讓出來的空間根本沒進到內容裡**——收了等於白收。
> 標題列本來就是滿版的，內容跟著滿版，左緣才對得齊
> （實測收折後對話清單與標題同在 x=78）。

* 提問以使用者頭像 + 粗體呈現
* 回答上方先出現檢索膠囊，再逐字串流答案
* 引註 `[n]` 為青綠色塊，點擊會**選取＋展開＋捲動**到對應來源卡
* 來源面板每筆顯示：編號、檔名、階段標籤、切片序、**距離**、內容摘要
* 面板底部常駐一則說明：「系統只依據上方來源作答」

> **距離要露出來。** V1 的教訓是相關性判斷不能靠距離門檻，
> 但把距離顯示給管理員看，是判斷索引品質最直接的線索。

#### 來源必須看得到全文（兩層）

摘要只顯示兩行，這是 **CSS 截斷，資料層不准先截**——
一旦後端先截，全文就永遠拿不回來了。`resolve_sources()` 回傳完整 `chunk.content`。

| 層 | 觸發 | 用途 |
|---|---|---|
| 卡片內展開 | 點卡片（再點收合） | 掃一眼，限高 230px 可捲 |
| 全文浮層 | 展開後的「⤢ 看全文」 | 真的閱讀，760px、Markdown 渲染、Esc 關閉 |

> **為什麼要兩層而不是只做展開？**
> 側欄只有 308px，長切片在裡面讀不了；而切片是 Markdown 片段，
> 含表格的切片用純文字顯示會糊成一團——V1 的 expander 就是這樣，
> 看得到字但看不懂結構。浮層裡表格要真的渲染成表格。

#### 對話紀錄併在這一頁，不獨立成頁

左欄就是對話清單：新對話、標題、訊息數、時間、刪除（需確認）。
點任一則載入該對話並**可直接接續提問**。

> **獨立一頁是錯的設計。** 那樣只能「看」不能「續」，
> 等於把個人的工作紀錄當成報表在做。接續本身不需要前端傳 history——
> 只要帶回原本的 `session_id`，後端 `get_messages()` 就會把整段前文餵給 Agent。

> **進頁面不要先建 session。** 先建會讓每次點開都留下一筆「0 則」的空對話，
> 清單很快就被垃圾洗掉（實測二十筆裡有一半是這樣來的）。
> 正確做法是**第一次送出問題時才建**；「新對話」只是清空前端狀態。

### 4.3 知識庫維護（管理端）

四個分頁：**文件 / 切片內容 / 解析狀態 / 檢索測試**。

切片內容頁是三欄工作台：

```
┌────────────┬──────────────────────────────┐
│ 切片清單    │ 切片標頭（檔名、字數、階段、維度）│
│ 296px      │ [原始文字 | 渲染後] 切換          │
│ 可搜尋      ├──────────────────────────────┤
│ 有關鍵字者  │ 內容（表格要真的渲染成表格）      │
│ 顯示標記    ├──────────────────────────────┤
│            │ ◎ 檢索關鍵字（標籤式輸入）        │
│            │   顯示調整前後的距離變化           │
└────────────┴──────────────────────────────┘
```

**關鍵字編輯做成標籤（tag input）而非純文字框**，
因為它本質是一組詞而不是一段話；並且要即時顯示
「距離 1.121 → 1.060」讓管理員知道剛才那筆調整有沒有效。

### 4.4 其餘頁面

| 頁面 | 重點 |
|---|---|
| 階段導覽 | 六階段時間軸；**共通文件另開一區**（V1 §5.3 的教訓：未歸屬階段的文件會沒有入口） |
| 文件庫 | 全部文件列表 + 線上閱讀 + 下載（**不可用 `os.startfile`**） |
| 模型與設定 | 模型選擇 + 工具調用能力判定 + VLM 自檢按鈕 |
| 帳號管理 | 建立、停用、重設密碼 |

---

## 5. API 設計

全部掛在 `/api`，回應一律 JSON；串流走 SSE。

### 5.1 認證

| 方法 | 路徑 | 說明 |
|---|---|---|
| POST | `/api/auth/login` | 回傳 JWT |
| GET | `/api/auth/me` | 目前使用者 |
| POST | `/api/auth/password` | 改自己的密碼 |
| GET | `/api/health` | 存活檢查（免認證） |

### 5.2 問答

| 方法 | 路徑 | 說明 |
|---|---|---|
| GET | `/api/chat/sessions` | 我的會話清單（問答頁左欄的資料來源） |
| POST | `/api/chat/sessions` | 開新對話（**第一次送出問題時才呼叫**，見 §9.8） |
| DELETE | `/api/chat/sessions/{id}` | 刪除對話（**強制 user_id 過濾**） |
| GET | `/api/chat/sessions/{id}/messages` | 訊息＋還原來源（**強制 user_id 過濾**） |
| POST | `/api/chat/ask` | **SSE 串流**，見下 |
| GET | `/api/chat/engine` | Ollama 狀態與工具調用能力 |

`/api/chat/ask` 直接把 `agent_service.answer()` 產出的事件轉成 SSE：

```
event: search   data: {"query":"DVT 散熱驗證","stage":"DVT","hits":6}
event: text     data: {"piece":"散熱驗證需在 "}
event: text     data: {"piece":"25°C、35°C…"}
event: done     data: {"answer":"…","chunk_ids":[12,7,3],"searches":1}
```

**這是 V2 唯一需要新寫的核心邏輯**，而且只是把既有的 generator
轉成 SSE 格式——Agent 的三層防護、緩衝規則完全不動。

### 5.3 階段、文件、管理

| 方法 | 路徑 | 說明 |
|---|---|---|
| GET | `/api/stages` | 六階段 + 文件數 + 索引統計 |
| GET | `/api/stages/{code}/documents` | 該階段文件 |
| GET | `/api/documents/unclassified` | **共通文件** |
| GET | `/api/documents/content?path=` | 線上閱讀（Markdown） |
| GET | `/api/documents/download?path=` | 下載原始檔 |
| POST | `/api/admin/index` | 觸發索引 |
| GET | `/api/admin/index-status` | 索引統計 |
| POST | `/api/admin/upload` | 上傳文件 |
| GET | `/api/admin/documents` | 知識庫檔案清單（**含尚未索引的**） |
| DELETE | `/api/admin/documents?path=` | 刪除檔案與它的所有痕跡 |
| GET | `/api/admin/chunks` | 切片清單（可搜尋） |
| GET | `/api/admin/chunks/{id}` | 單一切片全文＋關鍵字 |
| PUT | `/api/admin/chunks/{id}/keywords` | 更新關鍵字 → **立即重算向量** |
| POST | `/api/admin/search-test` | 檢索測試（回傳距離） |
| GET | `/api/admin/errors` | 解析狀態（`needs_vlm` 與 `failures` **分開兩類**） |
| GET/PUT | `/api/admin/models` | 模型設定 |
| GET | `/api/admin/users` | 帳號清單 |

> **文件的內容與下載走 query string 的 `path`，不是路徑參數的 `id`。**
> 文件是掃描檔案系統得到的，`path` 才是它的身分；用 `id` 會逼你為
> 「還沒進資料庫的檔案」硬造一個編號。
> 相對的，**後端必須擋路徑遍歷**——只接受 `sample_knowledge_base/` 底下的路徑。

---

## 6. 資料模型

**完全沿用 V1**，不新增也不修改任何資料表：

`users`、`documents`、`chunks`（含 `keywords`）、`chunk_keywords`、
`chat_sessions`、`chat_messages`、`stages`、`ingest_errors`、
`app_settings`、`vec_chunks`（sqlite-vec 虛擬表）。

結構相同，但 **V2 有自己的 `knowledge.db`**，是複製一份而不是共用。

> **不要讓兩版指向同一個資料庫。** 共用雖然省事，卻會讓兩版無法單獨打包分享，
> 而且任一邊改了設定（模型、關鍵字）另一邊會莫名其妙跟著變，出事時分不清是誰動的。
> 結構相同的好處仍在：V1 建好的 `knowledge.db` 可以**直接複製過來**用，不必重建索引。

---

## 7. 權限

V1 的界線一字不改，只是換一種傳遞方式：

* 登入後發 JWT，前端存在記憶體 + `httpOnly` cookie
* 每個受保護端點以 FastAPI 依賴注入取得 `current_user`
* 管理端點再經 `require_admin`
* **對話查詢一律以 `user_id` 過濾，管理員也不例外**

> **權限檢查在 API 層，前端隱藏選單只是體驗。**
> 前端是可以被繞過的，把 URL 改掉就能打到端點。

---

## 8. 實作路徑

每階段都有可執行的驗收，通過才往下走。

| 階段 | 內容 | 驗收 |
|---|---|---|
| 1 | FastAPI 骨架 + JWT 認證 | `/docs` 可開；未帶 token 回 401 |
| 2 | 沿用 `services/` 並包成 API | 以 curl 打 `/api/stages` 拿到六階段 |
| 3 | **SSE 問答** | 事件依序抵達；追問可用；無關問題被拒答 |
| 4 | 設計 token + 版面骨架 | 側邊欄依角色顯示；**可收折成圖示列且狀態記得住**；色彩全部來自 `tokens.css` |
| 5 | 問答頁 | 引註可點並高亮來源；串流逐字；**來源展得開也看得到全文**；左欄可接續舊對話；空對話不會被建出來 |
| 6 | 階段導覽 + 文件閱讀 | 表格正確渲染；共通文件有入口；可下載 |
| 7 | 管理端 | 切片三欄工作台；關鍵字編輯後距離改變 |
| 8 | 前端建置 + `start.bat` | 一鍵啟動；單一行程；純 ASCII 批次檔 |
| 9 | 測試 | 後端沿用 V1 測試；新增 API 層測試 |

---

## 9. V2 新增的實作陷阱

V1 §7 的陷阱**全部仍然適用**。以下是換成前後端分離後**新增**的：

### 9.1 SSE 而不是 WebSocket

問答串流是單向的（伺服器 → 瀏覽器），**用 SSE 就夠**，
不需要 WebSocket 的雙向連線與心跳管理。
但要注意兩件事：

* FastAPI 的 `StreamingResponse` 必須設 `media_type="text/event-stream"`
  並關閉緩衝（`X-Accel-Buffering: no`），否則反向代理會把串流整包才送出
* **每個事件都要 `\n\n` 結尾**，少一個換行整條串流就不會觸發

### 9.2 前端建置一次，由 FastAPI 提供靜態檔

**不要正式啟動時同時跑 `npm run dev` 與 uvicorn 兩個行程。**
`start.bat` 首次執行時 `npm install` + `npm run build`，
之後由 FastAPI 掛載 `frontend/dist`——**只需要一個行程**，
也不必處理跨埠 CORS。

### 9.3 `start.bat` 的要求與 V1 相同

專案路徑含中文，因此批次檔**必須 100% ASCII**，
且要設 `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8`。
這一條與 UI 框架無關，換成 React 照樣會踩。

### 9.4 引註高亮要用 chunk_id，不要用顯示編號

回答裡的 `[1]` 是**顯示用序號**，來源面板的排序可能因為
去重或排序方式而不同。前端把兩者對應起來時**必須以 `chunk_id` 為鍵**，
用序號硬對會在多輪對話後錯位。

### 9.5 串流中途的文字不要寫進對話紀錄

`agent_service` 的 `done` 事件才帶最終答案。
前端若把串流累積的 buffer 直接存起來，會少掉
「未檢索前緩衝的文字不輸出」這層防護（V1 §7.4.2）。
**一律以 `done` 事件的 `answer` 為準。**

### 9.6 LaTeX 在非 Streamlit 環境會原形畢露

模型偶爾會把溫度寫成 `$25^\circ	ext{C}$`、型號寫成 `$	ext{Tjmax}$`。
**V1 看起來沒問題，是因為 `st.markdown` 內建 KaTeX 會渲染掉。**
換成一般的 markdown 渲染器，畫面上就會出現一堆原始語法。

內容本身其實就不該有 LaTeX——這些是規格與數值，不是數學式。因此兩邊都要處理：

* `agent_service.SYSTEM_INSTRUCTION` 加一條：不要使用 LaTeX，溫度直接寫 25°C
* 前端 `stripLatex()` 做防禦性清理，處理模型偶爾的漏網之魚

> 這一類問題的通則：**框架幫你掩蓋掉的缺陷，換框架時會一次浮現。**
> 不要假設「V1 看起來對」等於「內容是對的」。

### 9.7 來源摘要只准在 CSS 截斷

側欄的來源卡片只顯示兩行，很容易順手在後端就把 `content` 切掉。
**不可以。** 一旦資料層先截，「看全文」就永遠做不出來——
而 V1 是有全文可看的（expander），少了它是功能倒退。

正確做法：`resolve_sources()` 回完整內容，
截斷交給 `-webkit-line-clamp: 2`，展開時改 `display: block` 解除。

`test_api.py` 有一項專門守這件事：
斷言歷史訊息帶回的來源長度未被截短、結尾沒有省略號。

### 9.8 對話紀錄不要獨立成一頁

獨立頁只能看不能續，等於把私人工作紀錄做成報表。
正確做法是併進問答頁左欄，點了就能接著問。

接續**不需要前端傳 history**——帶回原本的 `session_id` 就好，
後端 `get_messages()` 會把整段前文餵給 Agent。

連帶的坑：**進頁面不要先 `POST /api/chat/sessions`。**
先建會讓每次點開都留下一筆「0 則」的空對話，清單很快被垃圾洗掉。
要改成第一次送出問題時才建；「新對話」按鈕只清空前端狀態，不打 API。

### 9.9 `index.html` 一定要 `Cache-Control: no-cache`

`index.html` 的檔名沒有雜湊，但它裡面寫死了**帶雜湊的** JS／CSS 檔名。
瀏覽器一旦把它快取起來，重新建置後使用者仍然載入舊的 bundle，
而且完全看不出異狀——只會覺得「明明改了卻沒生效」。實測就這樣被騙過一次。

```python
return FileResponse(DIST / "index.html", headers={"Cache-Control": "no-cache, must-revalidate"})
```

`/assets` 底下的檔名本身帶雜湊，反而可以放心長期快取——**只有入口檔需要禁快取。**

### 9.10 不要對 `grid-template-columns` 加 transition

側邊欄收折看起來很適合加個動畫，但這個屬性每一幀都要重排整個版面；
更麻煩的是在沒有合成畫面的情境（背景分頁、隱藏視窗）動畫時鐘不會走，
屬性會**凍在起始值**——class 明明切換了，欄寬卻不動，看起來像壞掉。

收折本來就該是瞬間的。要動畫就挑 `opacity`／`transform` 這類合成器屬性。

### 9.11 刪除文件要清五個地方，少一個都會留問題

刪一份文件不是刪一列。要清乾淨的有五處：

| # | 對象 | 漏掉的後果 |
|---|---|---|
| 1 | `chunks` | 切片還在，檢索仍會撈到已刪的文件 |
| 2 | **`vec_chunks`** | sqlite-vec 虛擬表**不會因為 `chunks` 被刪就跟著消失**，向量成為孤兒 |
| 3 | `chunk_keywords` | 關鍵字備份留著，日後上傳**同檔名**的新檔案時，`restore_keywords()` 會把舊關鍵字套到新內容上 |
| 4 | `ingest_errors` | 「解析狀態」頁會一直顯示一個已經不存在的檔案 |
| 5 | **磁碟上的檔案** | 只刪資料庫，下次建索引又會被掃回來 |

> **文件清單要列磁碟，不要列 `documents` 資料表。**
> 上傳完、還沒建索引之前是沒有 Document 列的；只查資料表的話，
> 使用者剛上傳的檔案會看不見——而那正是「檔案到底存進去沒有」最需要確認的時刻。
> 因此刪除的鍵用**相對路徑**而不是 `doc_id`：未索引的檔案沒有 id，用 id 當鍵會有一半的檔案刪不掉。

> **路徑遍歷防護是必須的。** `path` 來自前端、不可信，要 `resolve()` 後確認仍在知識庫資料夾內，
> 否則 `../../` 就能刪掉專案裡的任何檔案。`test_api.py` 有三項測試守這件事。

### 9.12 複製資料庫時，裡面的絕對路徑不會跟著搬

V2 的 `knowledge.db` 是從 V1 複製過來的，但 `documents.file_path` 存的是**絕對路徑**，
複製後仍指向 `...\Leslie\sample_knowledge_base\`（V1 的資料夾）。

症狀很隱蔽：問答完全正常（切片內容存在資料庫裡），但

* 階段導覽的「線上閱讀」實際上讀的是 V1 的檔案
* V1 資料夾一旦刪除或搬移，V2 就壞
* 任何「以磁碟檔案比對資料表」的功能都會全部對不上

**複製 `knowledge.db` 之後必須改寫三張表的 `file_path`**：`documents`、`chunk_keywords`、`ingest_errors`。
改寫後要驗證每一列的路徑都真的存在，別只做字串取代就當作完成。

### 9.13 拒答斷言的關鍵字要列得夠寬

`test_api.py` 裡唯一比對 LLM 自由措辭的斷言就是「無關問題被拒答」，
也是唯一會間歇性失敗的一項。模型說「知識庫中**沒有**相關資訊」是正確拒答，
但窄的關鍵字表（查無／不足／無法／抱歉）接不住，於是偶爾誤報成 bug。

判準是**「有沒有表達出這件事不在知識庫裡」**，不是「有沒有用某個特定詞」。
同理，不要斷言答案裡一定出現 `[1]`——模型可能只引用 `[3]`、`[4]`。

---

## 10. 與 V1 的關係

* V1 **不刪除**。它是一份可運行、測試齊全的參考實作，
  也是 V2 出問題時的對照組。
* **兩版各自獨立**：各有自己的 venv、資料庫、services、範例知識庫，
  可以分開打包分享，也能同時啟動於不同埠（V1 為 8517、V2 為 8600）。
* 測試分工：
  * V1 `test_core.py`（83 項）驗 services 層——檢索、Agent、Grounding、對話隔離
  * V2 `test_api.py`（53 項）只驗 V2 新增的部分——JWT、角色守衛、SSE、對話清單與來源完整性、API 契約
* V2 若在某個功能上行為與 V1 不同，**以 V1 的測試為準**——
  那些測試是實際踩過坑後寫下的。

---
**文件結尾**
