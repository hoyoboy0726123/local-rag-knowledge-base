這是一份為「RD PM 研發管理知識庫 — 離線 AI 工具包」所編製的完整產品需求文件 (PRD)。本文件結合了您的「NUC 新產品開發管理辦法」骨幹架構與「可攜式、純內網、AI 檢索」的技術特性。

---

# 產品需求文件 (PRD)：RD PM 研發管理知識庫 — 離線 AI 工具包

## 0. 文件資訊
| 項目 | 內容 |
|------|------|
| 文件版本 | 1.0 |
| 建立日期 | 2026年7月28日 |
| 最後更新 | 2026年7月28日 |
| 文件狀態 | 草稿 |
| 文件擁有者 | [產品經理] |

---

## 1. 簡介

### 1.1 文件目的
本文件旨在詳述「RD PM 研發管理知識庫」最小可行產品 (MVP) 的功能規格、技術架構與實作範圍。此文件將作為開發團隊（React 前端、Node.js 後端）與專案利害關係人之間的溝通基礎，確保產品開發符合資安規範並滿足研發專案管理之需求。

### 1.2 產品背景
在研發專案管理中，SOP、宣導事項與 Lesson Learnt (經驗教訓) 往往分散在不同的網路硬碟、個人電腦或 Email 往返中。對於新進人員（New Hire）而言，學習曲線極高且容易重複犯錯。現有解決方案多依賴雲端工具（如 Notion, Confluence），但受限於公司資安政策，核心研發流程資料嚴禁外流至公有雲。因此，開發一個基於在地化 AI (Local AI) 的知識庫系統成為迫切需求。

### 1.3 產品願景
打造一個「裝在口袋裡的研發導師」。透過將繁雜的「NUC 新產品開發管理辦法」數位化與智能化，使每位 RD PM 都能在不連網的環境下，透過語意對話與結構化導覽，隨時隨地獲取精確的流程指導與歷史經驗。

### 1.4 MVP 目標
*   **結構化映射**：完成以 NUC 開發階段（Stage-Gate）為骨幹的數位目錄導覽。
*   **在地化 AI 檢索**：實現純內網環境下的檢索增強生成 (RAG)，支援自然語言問答。
*   **多格式解析**：支持 PDF、Excel、PPT 及文字化 Email 的內容讀取。
*   **引用來源透明化**：AI 回覆必須附帶原始文件來源與頁碼。
*   **零安裝可攜性**：提供解壓縮後即可執行的環境（Portable Bundle）。
*   **離線索引機制**：管理員能單機更新本地資料夾內容並重新產生 AI 索引。

---

## 2. 目標用戶

### 主要用戶
*   **初級/新進 RD PM**：需要快速了解 NUC 流程規範，避免在 EVT/DVT 等關鍵階段遺漏產出物。
*   **資深/管理級 RD PM**：需要快速回顧過去專案的 Lesson Learnt 以優化當前專案決策。

### 用戶特徵
*   **技術能力**：具備基本軟體操作能力，但不一定具備 AI 或程式開發背景。
*   **作業環境**：使用高規格商用筆電（16GB+ RAM），工作環境可能包含無網路的實驗室或高度限制的辦公區域。
*   **核心痛點**：找不到舊文件的最新版本、不知道某個異常狀況在過去是否有處理過、新人教育訓練佔用過多資深人員時間。

---

## 3. 核心問題與解決方案

*   **核心問題 1：知識碎片化，難以建立系統性認知**
    *   **MVP 解決方案**：建立「Stage-Gate 時間軸導覽器」，強行將碎片化的 SOP 與附件掛載在 NUC 管理辦法的各個階段（Concept, Plan, EVT, DVT, PVT, MP），讓使用者對流程有線性且整體的認知。
*   **核心問題 2：搜尋效率低下，傳統關鍵字搜尋無法處理非結構化資料**
    *   **MVP 解決方案**：導入「在地化向量數據庫 (Vector DB)」，透過語意搜尋讓使用者即使用詞不精確（例如：搜尋「散熱問題」而非「Thermal Module Issue」）也能找到相關文檔。
*   **核心問題 3：資安限制導致無法使用 ChatGPT 等強大工具**
    *   **MVP 解決方案**：整合「離線大型語言模型 (Local LLM)」，所有運算皆在 PM 本地電腦完成，確保數據完全不離開內網。
*   **核心問題 4：AI 回答的真實性疑慮 (幻覺問題)**
    *   **MVP 解決方案**：實作「Citation Tracing (來源追蹤)」，AI 的每一句總結都必須對應到本地資料夾中的特定文件片段，提供點擊即可開啟原始文件的功能。

---

## 4. 產品功能 (MVP 範圍)

### 4.1 NUC 階段導覽模組 (Stage-Gate Navigator)
*   **功能描述**：以視覺化時間軸呈現 NUC 管理辦法之核心流程。
*   **具體子功能**：
    *   **時間軸視圖**：左側選單依 Concept 到 MP 階段排列。
    *   **各階段產出物清單**：列出該階段必須完成的 Checklists 與 SOP。
    *   **文件掛載顯示**：在每個階段下自動顯示對應的歷史專案 Lesson Learnt。
    *   **階段快速跳轉**：點擊階段標籤，即時過濾右側 AI 問答的檢索範圍。

### 4.2 本地資料夾同步與掃描模組 (File Ingestion)
*   **功能描述**：監控並讀取使用者指定的「知識庫資料夾」。
*   **具體子功能**：
    *   **多格式解析引擎**：支援 PDF (標準文本/掃描件 Ocr 未必包含)、Excel (表格單元格讀取)、PPT (每頁幻燈片內容)、Email (TXT/EML 格式)。
    *   **後台索引監控**：自動偵測資料夾內的新增/修改文件。
    *   **手動重新索引 (Re-index)**：提供按鈕強制重新構建向量數據庫，以獲取最新資訊。
    *   **格式錯誤提示**：若有無法解析的文件，標記並告知用戶。

### 4.3 AI 智能對話中心 (Intelligent Chatbot)
*   **功能描述**：提供與知識庫內容互動的單一入口。
*   **具體子功能**：
    *   **自然語言提問**：支持繁體中文問答。
    *   **情境感知檢索**：AI 根據當前閱讀的階段，優先檢索該階段的文件。
    *   **對話紀錄儲存**：在本地暫存當前對話，關閉程式前可供回溯。
    *   **常用問題建議 (FAQ Suggestions)**：根據 NUC 流程自動生成如「本階段需要哪些測試報告？」等預設問題。

### 4.4 引用來源與聯動模組 (Source Referencing)
*   **功能描述**：解決 AI 回答的可信度問題。
*   **具體子功能**：
    *   **來源卡片**：在 AI 回覆下方列出參考文件名稱。
    *   **內容預覽**：點擊來源，於程式內彈窗顯示該文件的相關段落。
    *   **外部程式開啟**：點擊「開啟原始檔案」，直接調用系統預設程式（如 Excel, PDF Reader）並定位（若技術支持）。

### 4.5 管理員維護工具 (Admin Toolkit)
*   **功能描述**：讓知識庫管理者（您）能輕鬆更新內容並分發。
*   **具體子功能**：
    *   **一鍵封裝**：將索引完畢的向量數據庫封裝至壓縮檔。
    *   **版本管理**：在介面顯示「資料庫版本」與「更新日期」。
    *   **關鍵字屏蔽**：設定不希望被 AI 讀取或輸出的敏感詞過濾清單。

---

## 5. 用戶故事 (User Stories)

1.  **作為** 一名剛入職的 PM，**我想要** 在系統中輸入「如何進行試產申請」，**以便** 我不需要翻閱長達 100 頁的管理辦法就能知道流程。
2.  **作為** 負責 DVT 階段的 PM，**我想要** 系統主動列出過去三年在 DVT 階段發生過的散熱相關 Lesson Learnt，**以便** 我能提前在設計會議中提出預警。
3.  **作為** 管理者，**我想要** 直接將我的個人資料夾指定為數據源，**以便** 我能快速將我累積的 Email 經驗轉化為團隊可搜尋的知識。
4.  **作為** 資訊安全負責人，**我要求** 系統在拔掉網線的情況下仍能運作，**以便** 確保核心研發機密不外流。
5.  **作為** 一名 PM，**我想要** AI 在回答時告訴我這是出自哪份會議記錄，**以便** 我能找回當初的專案成員核實細節。
6.  **作為** 跨部門溝通者，**我想要** 查詢「各階段對應的協力部門」，**以便** 快速找到合適的聯絡窗口。
7.  **作為** 筆電空間有限的用戶，**我希望** 這個工具包解壓後即可執行，**以便** 我不需要安裝 Python 或 Node.js 等複雜開發環境。
8.  **作為** 頻繁外出開會的人員，**我希望** AI 的反應時間在 10 秒內，**以便** 我在會議空檔能快速獲得決策參考。

---

## 6. 非 MVP 範圍 (未來考量)

1.  **多人同時協作編輯**：未來可能支援多人同時在線修改知識庫註解。
2.  **自動生成專案報告**：根據知識庫內容自動撰寫週報草稿。
3.  **雲端/伺服器同步**：當公司開放部分私有雲資源時，實現多端同步。
4.  **圖片與語音辨識**：直接讀取白板討論的照片或會議錄音。
5.  **跨語言即時翻譯**：將英文規格書內容自動轉為中文問答。
6.  **主動式風險預測**：根據當前進度主動跳出風險警告。
7.  **整合 Jira/Trello**：將知識庫與具體工作任務關聯。
8.  **插件系統**：支援第三方開發的分析插件。
9.  **AI 模型微調 (Fine-tuning)**：根據公司的專業術語深度訓練專屬模型。
10. **移動端 App**：開發手機版本以便在工廠端使用。
11. **自動流程稽核**：由 AI 檢查當前專案產出物是否符合 NUC 規範。
12. **社群評分系統**：讓 PM 對 Lesson Learnt 的實用性進行投票。

---

## 7. 技術要求

### 7.1 後端框架 (Node.js)
*   **框架**：推薦使用 **Express** 結合 **Electron**。
*   **理由**：雖然是 Web 技術棧，但為了實現「可攜式工具包」，需要 Electron 將 Node.js 與 Chromium 打包成 `.exe`。Node.js 負責文件系統操作與調用本地模型 API。

### 7.2 資料庫
*   **關係型數據庫**：**SQLite**。
    *   **理由**：輕量、無需伺服器、單一檔案，符合可攜式需求，用於儲存文件元數據與開發階段定義。
*   **向量數據庫 (Vector DB)**：**ChromaDB** 或 **FAISS (Node.js bindings)**。
    *   **理由**：專為 AI 檢索設計，可儲存文本向量並進行相似度計算。

### 7.3 資料庫 ORM
*   **推薦**：**Prisma**。
    *   **理由**：強大的型別安全，與 Node.js 結合緊密，方便維護 SQLite 結構。

### 7.4 前端技術
*   **推薦**：**React + Tailwind CSS**。
    *   **理由**：React 適合構建複雜的單頁應用（如對話介面與時間軸），Tailwind CSS 能快速完成專業且現代感的 UI 界面設計。

### 7.5 其他整合
*   **AI 推理引擎**：**Ollama** 或 **Llama.cpp** 的 Node.js 接口。
*   **文件解析**：`pdf-parse`, `xlsx`, `mammoth` (DOCX), `pptx-parser`。
*   **打包工具**：**Electron Forge**，將整個 Web App 封裝為可攜式解壓縮包。

### 7.6 部署環境
*   **建議方式**：**Offline Portable Executable**。
*   **說明**：將所有 Node.js 依賴、React 靜態資源及輕量化 LLM 模型（如 Llama 3 8B Quantized）打包在同一個資料夾內。

### 7.7 版本控制
*   **建議**：使用 **Git** 進行程式碼管理。
*   **分支策略**：採用 `main` (穩定發佈版) 與 `develop` (開發版) 雙分支模式。

---

## 8. 成功指標

1.  **檢索效率提升**：用戶查找特定 SOP 或歷史案例的時間，從平均 15 分鐘（人工翻找）降低至 30 秒以內（AI 檢索）。
2.  **知識庫覆蓋率**：MVP 階段需完整覆蓋 NUC 管理辦法定義之 6 大階段及至少 50 份核心 SOP/Lesson Learnt。
3.  **回答準確率**：AI 針對事實性問題的回答，來源標記正確率需達到 90% 以上。
4.  **用戶滿意度**：在首批測試的 RD PM 中，超過 80% 的用戶認為此工具能有效減少詢問資深人員的次數。
5.  **資安零風險**：通過公司內網掃描測試，確認在運行過程中無任何外部網路請求。

---

## 9. 假設與約束

### 9.1 假設
1.  **硬體效能**：假設目標用戶的筆電至少具備 16GB 記憶體與 4 核心 CPU，以支持離線 LLM 運行。
2.  **資料品質**：假設原始 Excel, PPT 文件內容未加密且具備基本的文字可讀性。
3.  **用戶習慣**：假設 PM 願意改變搜尋習慣，嘗試使用對話式檢索而非手動翻資料夾。
4.  **模型能力**：假設 2026 年的量化開源模型（如 Llama 3 系列）在處理繁體中文商用邏輯上已足夠穩定。
5.  **管理支持**：假設研發部門主管支持此數位轉型計畫，並願意協助推廣。
6.  **文件格式統一性**：假設 NUC 管理辦法的版本更新頻率不高（如每季或每年一次），手動更新索引是可接受的。

### 9.2 約束
1.  **純離線環境**：系統絕對禁止存取任何雲端 API（如 OpenAI API），所有智慧化必須在本地端完成。
2.  **文件大小限制**：單個資料夾總量若超過 10GB，索引時間與搜尋速度可能會受到限制。
3.  **法律與授權**：所使用的開源模型與軟體庫必須符合商用許可，且不得包含具版權爭議的訓練數據。
4.  **可攜性限制**：工具包大小需控制在合理範圍（如 5GB 以內，含模型），以便透過公司內網或隨身碟傳輸。

---

**[文件結束]**