# ── 環境與快取 ──────────────────────────────────────────────
venv/
__pycache__/
*.pyc
node_modules/
.env

# ── 資料庫 ─────────────────────────────────────────────────
# knowledge.db 內含 documents.content ——**每份文件解析後的完整全文**。
# 只排除原始檔而不排除資料庫是沒有用的，內容一樣會被帶出去。
# 首次啟動時 seed_data.py 會重建帳號與範例文件，建索引即可還原。
*.db
*.db-wal
*.db-shm

# ── 使用者上傳的文件 ────────────────────────────────────────
# **整個知識庫資料夾都不進版控。**
#
# 它是使用者的資料，不是專案的一部分：
#   * 介面上傳的檔案會落在這裡，可能是公司內部文件
#   * 附一批特定領域的範例文件，會讓人以為這套系統只能用在那個場景
#
# 想要示範文件時執行：python seed_data.py --with-demo-docs
sample_knowledge_base/

# ── 選配模型 ───────────────────────────────────────────────
# 重排序模型 571 MB，不進版控。需要時由管理端「模型與設定」一鍵下載。
models/

# ── 其他 ───────────────────────────────────────────────────
*.log
.DS_Store
Thumbs.db
